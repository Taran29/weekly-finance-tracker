import { type NextPage } from "next";

const Profile: NextPage = () => {
  return (
    <div className="text-lg` flex min-h-screen w-screen items-center justify-center text-2xl text-white">
      Profile Page: In Progress :)
    </div>
  );
};

export default Profile;
